name: "avram"
source: "https://github.com/luckyframework/avram.git"
services:
  - postgreqsl
env:
  DATABASE_URL: "postgresql://postgres:postgres@localhost/avram"
commands:
  - createdb -h localhost -w -O postgres avram
  - crystal spec
formats:
  - crystal tool format
