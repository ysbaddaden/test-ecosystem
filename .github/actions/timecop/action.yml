---
name: timecop
inputs:
  shell:
    type: string
    default: bash
runs:
  using: composite
  steps:
  - run: git clone "https://github.com/crystal-community/timecop.cr.git" "timecop"
    shell: ${{ inputs.shell }}
  - run: shards install --skip-postinstall --skip-executables
    working-directory: timecop
    shell: ${{ inputs.shell }}
  - run: git apply --recount --ignore-space-change --ignore-whitespace $GITHUB_ACTION_PATH/project.patch
    working-directory: timecop
    shell: ${{ inputs.shell }}
  - run: crystal spec
    working-directory: timecop
    shell: ${{ inputs.shell }}
