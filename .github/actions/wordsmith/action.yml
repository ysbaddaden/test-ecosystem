---
name: wordsmith
inputs:
  shell:
    type: string
    default: bash
runs:
  using: composite
  steps:
  - run: git clone "https://github.com/luckyframework/wordsmith.git" "wordsmith"
    shell: ${{ inputs.shell }}
  - run: shards install
    working-directory: wordsmith
    shell: ${{ inputs.shell }}
  - run: crystal spec
    working-directory: wordsmith
    shell: ${{ inputs.shell }}
