---
name: Projects
"on":
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      crystal:
        type: string
        default: nightly
      shards:
        type: string
        default: nightly
      flags:
        type: string
        default: ""
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event.inputs }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}
env:
  CRYSTAL_FLAGS: ${{ inputs.flags }}
jobs:
  db-windows:
    runs-on: windows-latest
    steps:
    - run: git config --global core.autocrlf false
    - uses: ilammy/msvc-dev-cmd@v1
    - uses: actions/checkout@v5
    - uses: crystal-lang/install-crystal@v1
      with:
        crystal: ${{ github.event.inputs.crystal || 'nightly' }}
        shards: ${{ github.event.inputs.shards || 'nightly' }}
    - run: choco install sqlite
    - run: llvm-dlltool.exe -d sqlite3.def -D sqlite3.dll -l sqlite3-dynamic.lib
      working-directory: C:\ProgramData\chocolatey\lib\SQLite\tools
    - run: Add-Content -Path $env:GITHUB_ENV -Value "LIB=$env:LIB;C:\ProgramData\chocolatey\lib\SQLite\tools"
      shell: pwsh
    - name: 'Project: sqlite3'
      uses: ./.github/actions/sqlite3
